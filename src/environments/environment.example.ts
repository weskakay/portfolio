// Template: copy to environment.ts and environment.development.ts.
// Both copies are gitignored.

/** Build flag and the endpoint the contact form posts to. */
export const environment = {
  production: false,
  contactEndpoint: 'https://weskakay.de/contact_form_mail.php',
};
